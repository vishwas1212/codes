package concepts.custom.singleton;

// This class demonstrates the Enum Singleton pattern.
// Using an enum ensures a single instance, thread safety,
// and protection against serialization and reflection issues.
public enum CustomSIngletonEnum {

    INSTANCE;

    public void demo(){

    }
}
